# ============================================
# 前端环境变量配置（仅用于 Vercel 等云平台部署）
# ============================================

# 后端 API 地址（Vercel 部署时必须设置，Docker 部署时无需设置）
# 示例：VITE_API_BASE_URL=https://your-backend.example.com/api
# VITE_API_BASE_URL=

# ============================================
# 后端环境变量配置
# ============================================

# 后端端口
PORT=3030

# CORS 允许的前端域名（逗号分隔，不设置则允许所有来源）
# 生产环境建议设置，例如：ALLOWED_ORIGINS=https://feedback.example.com,https://www.example.com
# ALLOWED_ORIGINS=

# ============================================
# 数据库配置
# ============================================

# 数据库类型 (sqlite 或 mysql)
# DB_TYPE=mysql

# MySQL 数据库配置 (如果使用 MySQL)
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=your_db_user
# DB_PASS=your_db_password
# DB_NAME=vancefeedback

# ============================================
# 自动安装配置
# ============================================

# 首次启动时创建管理员账户
# ADMIN_USER=admin
# ADMIN_PASS=your_secure_password

# ============================================
# 注意事项
# ============================================
# 1. 复制此文件为 .env 并取消注释需要的配置项
# 2. 不要将包含敏感信息的 .env 文件提交到版本控制
# 3. 如果使用 docker-compose，可以在 docker-compose.separated.yml 中配置环境变量

# ============================================
# AI 功能配置 (可选)
# ============================================

# Google Gemini API Key (用于 AI 回复建议等功能)
# 获取地址: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY=your_gemini_api_key_here

# BigModel (Zhipu AI) API Key (智谱AI开放平台)
# 获取地址: https://open.bigmodel.cn/usercenter/apikeys
# BIGMODEL_API_KEY=your_bigmodel_api_key_here

# AI Provider (optional: 'gemini' or 'bigmodel')
# If not set, it will try Gemini first, then BigModel
# AI_PROVIDER=bigmodel

# ============================================
# Serverless 部署配置 (Vercel/AWS Lambda 等)
# ============================================

# JWT 密钥 (生产环境必须设置，用于用户认证)
# 建议使用随机生成的长密钥，例如：openssl rand -base64 32
# JWT_SECRET=your_secure_jwt_secret_here

# RSA 私钥 (Base64 编码，可选 - 系统会自动生成并存入数据库)
# 如果想使用固定密钥而非数据库存储，可以设置此项
# 生成方法: 
#   node -e "const NodeRSA = require('node-rsa'); const key = new NodeRSA({b:2048}); console.log(Buffer.from(key.exportKey('private')).toString('base64'))"
# RSA_PRIVATE_KEY=

# Serverless 环境标识 (可选，系统会自动检测 Vercel/AWS Lambda/Netlify)
# SERVERLESS=true
